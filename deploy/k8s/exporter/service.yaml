apiVersion: v1
kind: Service
metadata:
  name: metrics-exporter
  namespace: model-engine
  labels:
    app: metrics-exporter
spec:
  type: ClusterIP
  ports:
    - port: 9091
      targetPort: 9091
      protocol: TCP
  selector:
    app: metrics-exporter
